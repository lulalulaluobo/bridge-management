# 仅部署机保存，禁止提交。用 `openssl rand -base64 32` 生成。
APP_ENCRYPTION_KEY=
# `npx web-push generate-vapid-keys --json` 生成；没有 Push 配置时应用仍可显示临期状态。
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@lucc.fun
# 每日临期提醒任务的 Bearer 密钥。
CRON_SECRET=
