# 家庭冰箱管理 Agent｜需求基准

> 编译模式：COMPILE。本文锁定用户目标、边界和验收结果；技术方案由开发 Agent 决定。

## 1. 目标

为日常家庭用户交付一个优先在安卓手机使用、可安装为 PWA 的冰箱管理 Agent。用户像与熟人聊天一样，用文字、语音或照片管理食物采购批次；Agent 基于真实库存回答问题、追问必要信息，并在用户确认后更新数据。它还要帮助用户优先消耗临期和已开封食材，并给出符合家庭饮食偏好、过敏和禁忌的菜式建议。

最终交付不是演示页面：核心功能达到 L4，即在真实安卓 Chrome 上可完整操作、错误可恢复、数据刷新后仍成立。

## 2. 核心体验

### 采购入库闭环（首要）

用户说“刚买了两盒牛奶和一袋菠菜”。Agent 从当前对话中识别食物和数量；缺少会影响记录的信息时，只追问必要项，其他可采用已设置的类别默认有效期。Agent 展示可编辑的入库预览，用户确认后才写入。写入成功后，Agent 明确回复实际结果，首页库存同步更新；刷新页面后记录仍存在。

### 日常管理闭环

用户可以接着说“牛奶已开封”“菠菜放冷藏室”“鸡蛋吃了三个”或“把昨天买的酸奶删掉”。Agent 根据上下文查询并回复；遇到新增、修改、消耗、删除或批量操作时，先展示受影响批次和变更内容，等待确认后执行。用户可不经对话，直接在库存页手动编辑。

### 今天吃什么闭环

用户问“今天吃什么”。系统先从真实库存取得可用食材，再结合临期、已开封状态和家庭偏好生成至少三道菜。每道菜清楚说明将消耗的库存、缺少的配料和推荐原因；任何包含过敏原或违反禁忌的方案都不能出现。

## 3. 必须实现

| 功能 | 目标层级 | 最低可交付结果 |
| --- | --- | --- |
| 对话主入口与文字输入 | L4 | 用户可连续对话；Agent 能就当前任务追问或回复，且随时可切换为手动录入。 |
| 采购批次库存 | L4 | 保存名称、类别、数量、单位、购买日期、预计过期日、存放位置、开封状态；支持类别默认有效期和单批次覆盖。 |
| 受控操作与确认 | L4 | 查询可直接执行；每次新增、修改、消耗、删除和批量写入均显示预览并须确认，确认重复提交不产生重复记录。 |
| 用户自带模型 Key（BYOK） | L4 | 用户可在高级设置选择受支持的供应商与模型，通过 HTTPS 提交、验证、启用、轮换或删除自己的 Key；设置页仅显示掩码和状态，密钥不长期保留在前端。 |
| 库存状态 | L4 | 首页可区分已过期、即将过期和正常库存，并能定位、编辑、软删除某个批次。 |
| 语音与照片输入 | L3 | 语音转文字后进入同一对话流程；照片识别产出可逐项修改的候选批次清单。权限拒绝、识别失败或网络失败时，文字和手动编辑仍可完成任务。 |
| 菜式建议 | L4 | 至少返回三道菜，标出库存消耗、缺少配料和理由；过敏与禁忌优先于其他偏好。 |
| 临期提醒 | L3 | 用户能在应用内看到临期/过期状态，并在授权后收到提醒；未授权通知不影响库存使用。 |
| 用户安装与分发 | L4 | 非开发者可在安卓 Chrome 的 HTTPS 页面完成首次使用与安装；已安装应用有图标、独立窗口和明确的更新/安装说明。 |

## 4. 暂不实现

- 营养分析、自动采购、智能冰箱连接。
- 复杂多人权限。
- 原生 APK、应用商店发布。
- 未经用户确认的 LLM 写入或自动删除。

## 5. 硬约束

- 平台优先级为安卓手机和 Chrome；Web 端先完成，再作为 PWA 安装。相机和麦克风仅在安全上下文中使用。
- 用户是普通家庭用户，不要求理解数据库、安装包、模型或命令行。
- LLM 只能输出受约束的结构化意图和对话内容，不能访问数据库、生成或执行 SQL，也不能绕过确认流程。
- 用户自带 Key 只可通过 HTTPS 发送给后端；后端以可轮换的加密密文保存，禁止写入前端持久化存储、数据库明文字段、日志、分析事件、错误信息或备份导出。删除 Key 必须使密文立即不可再用，轮换后旧 Key 不再用于新请求。
- 首版仅支持服务端预置并校验过的供应商和模型，不接受任意 API 地址作为代理目标。每个已配置模型应声明可用能力；缺少语音或视觉能力时，必须提示并回退至文字/手工流程。
- 后端必须校验所有写操作、拒绝无效字段和非法数量，并防止重复提交；删除优先软删除。
- 语音、图片和通知均为可选能力：失败、拒绝权限或不支持时，不能阻断文字输入和手工编辑。
- 菜式建议必须以过敏和禁忌为最高优先级；推荐不是医疗或食品安全判断。

## 6. 可观察行为

- 对话界面只把“已完成”用于后端已返回成功的操作；确认前只称为“待确认”。
- 预览卡让用户看清食物、数量、日期、位置、开封状态以及受影响批次，并能在确认前修改或取消。
- 同一确认动作被快速点击两次，库存只变化一次。
- 用户完成 Key 验证后，设置页只显示供应商、模型、掩码、状态和更新时间；刷新、重新登录和导出数据都不能取得完整 Key。
- 用户轮换或删除 Key 后，旧 Key 不能再用于后续模型请求；模型不可用时，Agent 清晰提示并保留库存的非智能操作入口。
- 用户拒绝麦克风或相机权限后，界面说明替代方法而非停留在加载状态。
- 当同名食物有多个批次且无法判断目标时，Agent 询问用户选择，不擅自修改其中一个。
- 用户从安装入口进入时无需了解 PWA 概念；无法自动触发安装时，页面给出当前浏览器可执行的安装步骤。

## 7. 验收标准

1. 在安卓 Chrome 中，用户从首次打开页面到安装应用可独立完成；已安装应用能从桌面启动。
2. 用户以文字输入一条包含至少两种食物的采购描述，得到可编辑预览；确认后首页显示对应批次，刷新后仍存在。
3. 用户连续补充开封状态或存放位置时，Agent 将补充信息应用到正确的待确认批次；用户取消时库存不变。
4. 库存查询无需确认；新增、修改、消耗、删除和照片批量入库均无法绕过确认。重复确认不会造成重复写入，删除后的批次不出现在默认库存中。
5. 用户可提交一把受支持供应商的 Key 并完成验证；设置页只显示掩码。轮换为新 Key 后请求使用新 Key；删除后请求不再使用旧 Key，且完整 Key 不出现在页面、日志、导出文件或错误响应中。
6. 缺少类别默认有效期时，Agent 或界面要求用户补充，而不是编造日期；默认有效期被使用时，预览中可见其计算结果。
7. 分别拒绝麦克风权限、拒绝相机权限和模拟识别失败，均能改用文字和手工编辑完成同一笔入库。
8. 对含有临期、已开封食材的库存询问“今天吃什么”，返回至少三道菜；每道菜列出消耗库存、缺少配料和理由，且不包含已配置过敏原或禁忌。
9. 已过期、即将过期、正常批次在首页呈现正确分组；用户可在应用内看到提醒状态，通知未授权时不影响其他功能。
10. 开发 Agent 在真实安卓设备上记录并报告安装、文字对话确认写入、相机、麦克风、提醒与失败降级的实际测试结果；未通过项不得标为完成。

## 8. 自主权边界与未明确事项

开发 Agent 可自行选择技术栈、数据存储、模型服务、部署平台、认证方式、数据模型和界面细节，只要满足上述行为、隐私和验收要求。应优先采用低复杂度、可维护、可部署的方案。

开发 Agent 不得改变对话优先、确认写入、采购批次、过敏/禁忌优先、安卓 PWA 分发和真实真机验收这些范围。首版的家庭账号/共享方式尚未指定；可选择最简单且不引入复杂权限的方案，并在实施记录中说明。

> 在不违反目标、硬约束和验收标准的前提下，你可以自主选择技术栈、依赖、文件结构和实现方式。优先采用简单、可维护、可验证的方案。不要因缺少非关键细节而停止执行。本轮只完成指定闭环，不要增加未接通的页面。功能只有在实际运行并通过验收后才算完成。完成后逐项报告测试结果、未通过项和剩余风险。
